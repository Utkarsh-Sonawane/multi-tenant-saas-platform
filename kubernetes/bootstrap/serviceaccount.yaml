apiVersion: v1
kind: ServiceAccount
metadata:
  name: db-bootstrap-secrets-reader
  namespace: tenant-a
  annotations:
    # Terraform/IAM must supply an IRSA role allowed to read only
    # /multi-tenant/prod/rds/* and /multi-tenant/prod/{config-db,tenant-*}/db/password.
    eks.amazonaws.com/role-arn: REPLACE_WITH_BOOTSTRAP_IRSA_ROLE_ARN
