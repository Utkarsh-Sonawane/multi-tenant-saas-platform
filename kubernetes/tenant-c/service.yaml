apiVersion: v1
kind: Service
metadata:
  name: tenant-c-service
  namespace: tenant-c
  labels:
    app: multi-tenant-saas
    tenant: tenant-c
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-path: "/healthz"
spec:
  type: ClusterIP
  selector:
    app: multi-tenant-saas
    tenant: tenant-c
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
